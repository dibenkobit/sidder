{
  "name": "sidder-example-node-postgres",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "db:up": "docker compose up -d --wait",
    "db:down": "docker compose down",
    "seed:status": "sidder status",
    "seed:dry-run": "sidder run --dry-run",
    "seed": "sidder run"
  },
  "dependencies": {
    "pg": "^8.0.0"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/pg": "^8.0.0",
    "sidder": "latest",
    "typescript": "latest"
  }
}
